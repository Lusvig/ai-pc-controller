# ===========================================
# AI PC Controller - Production Dependencies
# ===========================================
# This file contains ONLY packages that install without requiring
# C compilation or Visual Studio Build Tools on Windows.
# ===========================================

# -------------------------------------------
# AI PROVIDERS (All install cleanly)
# -------------------------------------------
ollama>=0.3.0
google-generativeai>=0.5.0
groq>=0.5.0
openai>=1.30.0

# -------------------------------------------
# HTTP / ASYNC
# -------------------------------------------
httpx>=0.27.0
aiohttp>=3.9.0
requests>=2.31.0

# -------------------------------------------
# GUI FRAMEWORK
# -------------------------------------------
customtkinter>=5.2.0
Pillow>=10.3.0
pystray>=0.19.5
darkdetect>=0.8.0

# -------------------------------------------
# PC CONTROL - Core
# -------------------------------------------
pyautogui>=0.9.54
pygetwindow>=0.0.9
keyboard>=0.13.5
mouse>=0.7.1
psutil>=5.9.8
pyperclip>=1.8.2

# -------------------------------------------
# PC CONTROL - Windows Specific
# -------------------------------------------
pywin32>=306; platform_system == "Windows"
pycaw>=20240210; platform_system == "Windows"
comtypes>=1.3.1; platform_system == "Windows"
wmi>=1.5.1; platform_system == "Windows"
screen-brightness-control>=0.22.0

# -------------------------------------------
# VOICE - Core (These work without build tools)
# -------------------------------------------
SpeechRecognition>=3.10.4
pyttsx3>=2.90

# -------------------------------------------
# VOICE - Optional (May need separate install)
# PyAudio often needs special handling on Windows
# Install separately with: pip install pipwin && pipwin install pyaudio
# Or download wheel from: https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
# -------------------------------------------
# pyaudio>=0.2.14; platform_system == "Windows"

# -------------------------------------------
# AUDIO - Pure Python alternatives
# -------------------------------------------
sounddevice>=0.4.6
scipy>=1.13.0
numpy>=1.26.0

# -------------------------------------------
# WEB AUTOMATION
# -------------------------------------------
selenium>=4.20.0
webdriver-manager>=4.0.1
beautifulsoup4>=4.12.3

# -------------------------------------------
# DATABASE
# -------------------------------------------
SQLAlchemy>=2.0.30
alembic>=1.13.1

# -------------------------------------------
# CONFIGURATION
# -------------------------------------------
pyyaml>=6.0.1
python-dotenv>=1.0.1
pydantic>=2.7.0
pydantic-settings>=2.2.0

# -------------------------------------------
# UTILITIES
# -------------------------------------------
loguru>=0.7.2
apscheduler>=3.10.4
watchdog>=4.0.0
cryptography>=42.0.5
humanize>=4.9.0
rich>=13.7.1
click>=8.1.7
tqdm>=4.66.2

# -------------------------------------------
# WINDOWS UTILITIES
# -------------------------------------------
winshell>=0.6; platform_system == "Windows"
pywin32-ctypes>=0.2.2; platform_system == "Windows"

# ===========================================
# PACKAGES REMOVED (Require C Build Tools):
# ===========================================
# webrtcvad - Not critical, voice works without it
# netifaces - Replaced with pure Python solution
# pvporcupine - Wake word detection (optional paid service)
# monitorcontrol - DDC/CI monitor control (limited use)
# speedtest-cli - Can be added later if needed
# ===========================================